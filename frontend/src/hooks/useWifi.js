//hooks/useWifi.js
//(OBS: só funciona nativo em mobile/app, não browsers convencionais!)

export const useWifi = () => {
  // No browser só via API experimental (Web Wifi, quase sempre indisponível). 
  // Em apps móveis, pode-se usar plugins cordova/capacitor.
  return null;
};

